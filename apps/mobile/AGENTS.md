# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# UI/UX

Before building or changing any UI (screens, components, animations, typography), read
`DESIGN.md` and follow its principles. It encodes the app's design rules (baseado no
trabalho do Emil Kowalski, traduzido para React Native + Reanimated).
